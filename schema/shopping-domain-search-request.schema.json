{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schemas.sil.com/shopping-domain-search-request.schema.json",
  "title": "sil shopping_domain_search — request",
  "type": "object",
  "required": [
    "q"
  ],
  "properties": {
    "q": {
      "type": "string",
      "minLength": 1,
      "maxLength": 200,
      "description": "Sends the item in the buyer's own words."
    }
  },
  "additionalProperties": false
}
